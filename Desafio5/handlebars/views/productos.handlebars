<div>
    <h1 class="text-center mb-4">Lista de productos</h1>
    {{#if products}}
        <div class="d-flex align-items-center justify-content-center border-bottom">
            <div class="container row">
                <div class="col">
                    Nombre
                </div>
                <div class="col">
                    Precio
                </div>
                <div class="col">
                    Imagen
                </div>
            </div>
        </div>

        {{#each products}}
            <div class="container border-bottom">
                <div class="row">
                    <div class="col d-flex align-items-center">
                    <p>{{this.nombre}}</p>
                    </div>
                    <div class="col d-flex align-items-center">
                    <p>{{this.precio}}</p>
                    </div>
                    <div class="col d-flex align-items-center">
                    <img class="img-thumbnail" src="{{this.url}}" alt="{{this.nombre}}">
                    </div>
                </div>
            </div>
        {{/each}}
    {{else}}
    <h5 class="text-center" >No se encontraron productos..</h5>
    {{/if}}

    <a class="d-flex align-items-center justify-content-center" href="./productos">
        <button type="button" class="btn btn-success m-3">Agregar Productos</button>
    </a>
</div>